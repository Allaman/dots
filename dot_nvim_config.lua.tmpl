-- https://github.com/Allaman/nvim/
return {
  plugins = {
    kustomize = {
      dev = true,
    },
    lazy = {
      dev = {
        path = "$HOME/workspace/github.com/allaman/",
      },
    },
    telescope = {
      fzf_native = {
        enable = true,
      },
    },
  },
}
